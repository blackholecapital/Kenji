(() => {
  const $=(s,r=document)=>r.querySelector(s);
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  async function api(path){const r=await fetch(path,{headers:{accept:"application/json"}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d;}
  function toast(m){const el=$("#toast");if(!el)return;el.textContent=m;el.classList.add("show");clearTimeout(el._p8);el._p8=setTimeout(()=>el.classList.remove("show"),2800);}
  function css(){if($("#pass8Css"))return;const s=document.createElement("style");s.id="pass8Css";s.textContent=`.p8-panel{margin-top:16px}.p8-checks{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:12px}.p8-check{border:1px solid rgba(255,255,255,.08);border-radius:11px;padding:9px;background:rgba(255,255,255,.025)}.p8-check b,.p8-check span{display:block}.p8-check span{font-size:10px;color:var(--muted,#98a2b3);margin-top:3px}.p8-check.ok{border-color:rgba(85,217,139,.28)}.p8-check.bad{border-color:rgba(255,110,90,.28)}@media(max-width:800px){.p8-checks{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s);}
  function install(){css();const host=$("#view-scale");if(!host){setTimeout(install,80);return;}if($("#pass8Rehearsal"))return;const panel=document.createElement("article");panel.id="pass8Rehearsal";panel.className="panel p8-panel";panel.innerHTML=`<div class="panel-head"><div><p class="eyebrow">FULL-STACK REHEARSAL</p><h3>One-click preflight, zero provider traffic</h3></div><button id="p8Run" class="primary small-btn">Run system preflight</button></div><p class="copy">Checks the data plane, voice runtime, SMS, email, nurture, orchestrator and the new queue-backed video worker. This does not place calls, send messages or open an avatar room.</p><div id="p8Checks" class="p8-checks"><span class="copy">Run the preflight when the demo stack is ready.</span></div>`;host.appendChild(panel);$("#p8Run").addEventListener("click",run);}
  function card(name,ok){return `<div class="p8-check ${ok?"ok":"bad"}"><b>${esc(name)}</b><span>${ok?"ready":"check lane"}</span></div>`;}
  async function run(){const out=$("#p8Checks"),btn=$("#p8Run");btn.disabled=true;out.innerHTML="<span class='copy'>Checking every runtime lane…</span>";try{const d=await api("/api/pass8/rehearsal"),c=d.checks||{};out.innerHTML=[card("Data plane",c.data),card("Voice",c.voice),card("SMS",c.sms),card("Email",c.email),card("Nurture",c.nurture),card("Orchestrator",c.orchestrator),card("Video queue",c.video)].join("");toast(d.ok?"Full stack preflight is green":"Preflight found a lane to inspect");}catch(e){out.innerHTML=`<span class="error">${esc(e.message)}</span>`;}finally{btn.disabled=false;}}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",install,{once:true});else install();
})();
